<h1>My tasks</h1>
<p>Now is <%= Time.now %></p>

<ul>

  <% @tasks.each do |task| %>
  <li><%= link_to task.title, task_path(task) %> <%= link_to "Edit", edit_task_path(task) %>
    <%= link_to "Delete", task_path(task), method: :delete, data: { confirm: "Are you sure?" }   %>
  </li>
  <h2></h2>

   <% end %>
</ul>


 <h3><%= link_to "Create new task", task_new_path() %></h3>







